version: '3.8'
services:
    redis:
        image: redis:alpine
        container_name: linkflow-redis
        ports:
            - '6379:6379'
        volumes:
            - redis-data:/data
        restart: unless-stopped

volumes:
    redis-data:
